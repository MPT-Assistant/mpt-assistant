import IConfig from "./IConfig";

const config: IConfig = {
    vk: { token: "" },
    db: { mongo: {
        protocol: "",
        address: "",
        login: "",
        password: "",
    }, },
};

export default config;
